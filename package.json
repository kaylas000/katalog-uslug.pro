{
  "name": "katalog-uslug.pro",
  "private": true,
  "description": "Статический каталог услуг; шапка/подвал собираются из partials",
  "scripts": {
    "build:partials": "node scripts/generate-partials.mjs",
    "build:layout": "node scripts/inject-layout.mjs",
    "build:site": "node scripts/generate-partials.mjs && node scripts/inject-layout.mjs"
  }
}
