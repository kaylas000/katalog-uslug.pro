# behavior.md (Kiro Steering)

## Tool use rules (MCP)
- Before making changes, always read the relevant files first.
- When planning how to do something, use the `sequentialthinking` tool to plan several steps ahead.
- Track long-term work context in the knowledge graph using the memory tools.

## Memory routine
- At the start of each user request:
  - create a `user_request` entity with a short title and timestamp.
- After completing work:
  - add observations: what changed, which files, what commands/tests were run, and what remains.
